import { Component } from '@angular/core';
import { NavController } from 'ionic-angular';

@Component({
    selector: 'page-poetry',
    templateUrl: 'poetry.html'
})
export class PoetryPage {
    constructor(public navCtrl: NavController) {
    }
}
